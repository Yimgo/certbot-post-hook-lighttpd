# certbot-post-hook-lighttpd

certbot post-hook to update lighttpd SSL configuration with certificate from Let's Encrypt.

## Usage

```bash
$ certbot-post-hook-lighttpd yimgo.fr
```

## Configuration

Get a look at `crontab` and `lighttpd.conf`.
